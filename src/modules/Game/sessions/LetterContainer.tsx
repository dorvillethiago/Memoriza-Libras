import LetterButton from "../components/LetterButton"
export default function LetterContainer() {
    return (
        <div>
            <LetterButton letter="A" onClick={() => {}}/>
        </div>
    )
}